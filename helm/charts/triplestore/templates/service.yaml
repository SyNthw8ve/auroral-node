apiVersion: v1
kind: Service
metadata:
  name: "auroral-triplestore"
  labels:
    {{- include "triplestore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "triplestore.selectorLabels" . | nindent 4 }}
