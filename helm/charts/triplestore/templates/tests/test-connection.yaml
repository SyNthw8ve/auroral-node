apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triplestore.fullname" . }}-test-connection"
  labels:
    {{- include "triplestore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "triplestore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
