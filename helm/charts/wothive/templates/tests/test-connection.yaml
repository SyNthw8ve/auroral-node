apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wothive.fullname" . }}-test-connection"
  labels:
    {{- include "wothive.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wothive.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
